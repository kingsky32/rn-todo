const blackColor = '#262626';

const whiteColor = '#ffffff';

const greyColor = '#FAFAFA';

const darkGreyColor = '#999999';

const lightGreyColor = '#F2F2F2';

const blueColor = '#007aff';

const paddingHorizontal = 16;

const borderRadius = 10;

export default {
  blackColor,
  whiteColor,
  greyColor,
  darkGreyColor,
  lightGreyColor,
  blueColor,
  paddingHorizontal,
  borderRadius,
  boxStyle: `
    border-radius: ${borderRadius}px;
    padding: 12px;
  `,
};
